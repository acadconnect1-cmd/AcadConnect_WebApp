import { AboutClient } from './AboutClient'

export const metadata = {
  title: 'About AcadConnect | Our Mission & Legacy',
  description: 'Learn how AcadConnect is bridging the gap in higher education by connecting world-class universities with verified faculty candidates.',
}

export default function AboutPage() {
  return <AboutClient />
}
