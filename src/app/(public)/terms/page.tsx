import { TermsClient } from './TermsClient'

export const metadata = {
  title: 'Terms of Service | AcadConnect',
  description: 'Read the terms of service governing candidate profiles, institutional job listings, search committee actions, and membership requirements on AcadConnect.',
}

export default function TermsPage() {
  return <TermsClient />
}
